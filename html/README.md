#Lưu ý cực kì quan trọng
#Cấu trúc web sẽ là Header + Web + Footer
#Nên trong các trang html không cần mở thẻ HTML hay BODY
#nên nhớ nó đã mặc định ở trong body
#và trên hết không được dùng chung tên class với nhau để tránh format bị đè lên nhau
#nếu được thì đặt tên class dạng "<Tên Trang HTML>+<Tên class>"
#ví dụ : "TrangChu_menu" hoặc "TC_menu"
